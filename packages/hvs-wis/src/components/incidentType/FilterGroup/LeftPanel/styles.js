import { makeStyles } from "@material-ui/core";

const styles = makeStyles((theme) => ({
  root: {
    flex: "50%",
    borderRight: `3px solid ${theme.hv.palette.atmosphere.atmo2}`,
  },
}));

export default styles;
